# Simple-Flask-Application-
A Flask application is a Python web application that is used to create dynamic websites. It connects with a database to perform CRUD (Create, Read, Update, Delete) operations and display data on web pages.
